const nameTS: string = "thenam";

// co goi y code =! javascript
console.log("length =", nameTS.length);
console.log("upperCase =", nameTS.toUpperCase());

// cac loai data-type voi TS:
//Primitive data-type: String, number, boolean, null, undefined, symbol, bigint
//References data-type: Object, Array, Functions
